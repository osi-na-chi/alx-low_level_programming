#include "main.h"
#include <ctype.h>

/**
 * _isdigit - function that checks for a digit
 *
 * @c: character to print
 *
 * Return: 0 on success and 1 on fail
 */

int _isdigit(int c)
{
int i = _isdigit(c);
if (i > 0)
{
return (1);
}
else
{
return (0);
}
}
